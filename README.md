# Nihar_2025B5PS1281H_avionics

Task1-its the python code for measuring the depth data to be honest with you guys i don't have any prior experience in python  so i used Gemini and Claude to solve this question ,and i saw that there are multiple ways to extract data from the CSV.file but the panda library make it very easy to extract . recently in my physics optics course they started teaching python to plot static graphs but it doesn't help here and i saw the video u guys recommended i saw that to understand the code for animating the graph .while i used ai models i also came across a new library scipy apparently that helps us to make the graph smoother and also tackle the real world noise porblem. For now I plan to explore more python and its libraries because i found that is a cool language to learn(btw its far better than c to learn :)) which makes it beneficial for me in future and also for the club(if selected ).

->first the code reads the CSV.file.

->next the code is made to smooth the graph using the rolling window.

->then we used the animation library to animate the graph.

->and finally if we run this we would get the graph which has the smoothed graph(uses scipy librabry) and also a point where there is correction.


Task2-the electronic stuff, so even in this i am not that much experience but why did i choose this because from approximately last year  i have developed interest in electronics and since i am not experienced in both but I was doing this task for a little longer time i was following the You tube videos of the Paul M C Whorter and a little help from my friend who helped me in this btw he also applied for SEDS. so i have used regular lcd rather than the I2C. 

-> the code is mostly using the digital logic o and 1's so it has a lot of if else statements.

-> first the code will detect the problem first Storm or Charbydis.

-> then the code is checks is the ship anchored or sailing then accordingly checks for the problem.

-> the the result in displayed on the lcd.

-> and when the ship gets wrecked we need to simulate it again.

so this is how the code works here.
